# Core Flask dependencies (pinned for reproducibility)
Flask==3.1.2
Werkzeug==3.1.3
Jinja2==3.1.6
MarkupSafe==3.0.3
itsdangerous==2.2.0
click==8.3.0
blinker==1.9.0
colorama==0.4.6

# Security - XML parsing protection
defusedxml==0.7.1

# Production server (minimum version for compatibility)
gunicorn>=21.0.0

# Testing (minimum versions)
pytest>=8.0.0
pytest-cov>=4.1.0
